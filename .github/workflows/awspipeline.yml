name: AWS Pipeline
on: [push]
jobs:
  myDeployment:
    uses: samuelhornby/githubactions/.github/workflows/templates/awstemplate.yml@main
    with:
      env: 'dev'
      location: 'here'
    secrets: 
      TF_TOKEN: ${{ secrets.TFCLOUD_TOKEN }}