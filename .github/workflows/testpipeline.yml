name: GitHub Actions
on: [push]
jobs:
  myDeployment:
    uses: samuelhornby/githubactions/.github/workflows/template.yml@main
    with:
      env: 'dev'
      location: 'here'
    # secrets: # probably wouldn't use this, just get from KV
    #   vmPassword: ${{ secrets.vmPassword }}